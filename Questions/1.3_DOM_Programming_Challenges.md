# DOM Programming Challenges — Complete Curriculum

## Level 1 — Understanding the DOM (Challenges 1–15)

1. Select an element with `id="title"` and log its `textContent` to the console.
2. Select an element with `id="description"` and change its `textContent` to `"Hello, DOM!"`.
3. Select an element with `id="counter"` and set its `textContent` to the number `0`.
4. Select an element with `id="greeting"` and replace its text with the current time as a string.
5. Select an element with `id="username-display"` and update it to show the value of a variable called `userName`.
6. Select an element with `id="status"` and toggle its text between `"Online"` and `"Offline"` based on a boolean variable.
7. Select an element with `id="price"` and update its text to show a price value formatted with a dollar sign.
8. Select an element with `id="notification-count"` and increment its text by 1 each time a function runs.
9. Select an element with `id="error-message"` and clear its text content entirely.
10. Select an element with `id="score"` and set its text to the result of a simple math expression.
11. Select an element with `id="welcome"` and update it to include both a first name and last name variable.
12. Select an element with `id="temperature"` and append the degree symbol after a numeric value.
13. Select an element with `id="loading"` and change its text to `"Loading..."` and then after 2 seconds change it to `"Done!"`.
14. Select an element with `id="badge"` and update its text based on the length of an array.
15. Select an element with `id="output"` and concatenate three different variables into its text content.

---

## Level 2 — Event Fundamentals (Challenges 16–35)

16. Add a `click` event listener to a button with `id="click-btn"` that logs `"Clicked!"` to the console.
17. Add a `click` event listener to a button with `id="color-btn"` that changes the background color of the page.
18. Add a `dblclick` event listener to a div with `id="box"` that toggles its border color.
19. Add a `mouseenter` event listener to an element with `id="hover-area"` that changes its text to `"Hovering!"`.
20. Add a `mouseleave` event listener to the same element that reverts the text back.
21. Add a `keydown` event listener to the document that logs which key was pressed.
22. Add a `keydown` event listener to an input with `id="name-input"` that prevents the letter `"e"` from being typed.
23. Add a `keyup` event listener to an input with `id="search"` that logs the current value after each keystroke.
24. Add an `input` event listener to an input with `id="email"` that validates whether it contains an `@` symbol.
25. Add a `change` event listener to a select element with `id="country"` that displays the selected value in a paragraph.
26. Add a `click` event listener to a list of buttons with class `"option"` that logs the `textContent` of the clicked button.
27. Add a `click` event listener to an element with `id="toggle-btn"` that toggles a boolean variable and logs it.
28. Add a `keydown` event listener that detects when the Enter key is pressed and triggers a function.
29. Add a `mouseenter` and `mouseleave` pair to an element that changes its size.
30. Add an `input` event listener to a textarea with `id="bio"` that counts and displays the number of characters typed.
31. Add a `change` event listener to a checkbox with `id="agree"` that enables or disables a submit button.
32. Add a `click` event listener to a button with `id="random-btn"` that generates and displays a random number.
33. Add a `keydown` event listener that only responds to number keys and appends them to a display element.
34. Add a `dblclick` event listener to an image with `id="photo"` that toggles between two different `src` values.
35. Add a `click` event listener to a button with `id="reset-btn"` that clears all text inputs on the page.

---

## Level 3 — DOM Manipulation (Challenges 36–55)

36. Select an element with `id="headline"` and change its `textContent` to a new string.
37. Select an element with `id="card"` and change its `innerHTML` to include a heading and a paragraph.
38. Select an element with `id="link"` and change its `href` attribute to `"https://example.com"`.
39. Select an element with `id="avatar"` and change its `src` attribute to a new image URL.
40. Select an element with `id="box"` and change its `style.backgroundColor` to `"blue"`.
41. Select an element with `id="text"` and change its `style.fontSize` to `"24px"`.
42. Select an element with `id="message"` and add the class `"highlight"` to it.
43. Select an element with `id="message"` and remove the class `"highlight"` from it.
44. Select an element with `id="message"` and toggle the class `"visible"` on it.
45. Select an element with `id="banner"` and change its `style.display` to `"none"`.
46. Select an element with `id="banner"` and change its `style.display` back to `"block"`.
47. Select an element with `id="container"` and change its `style.width` to `"50%"`.
48. Select an element with `id="item"` and change its `style.opacity` to `0.5`.
49. Select an element with `id="nav"` and change its `style.position` to `"fixed"`.
50. Select an element with `id="title"` and change its `style.color` based on a variable value.
51. Select an element with `id="list"` and change its `style.listStyleType` to `"none"`.
52. Select an element with `id="button"` and change its `style.borderRadius` to `"10px"`.
53. Select an element with `id="panel"` and change its `style.boxShadow` to a custom shadow string.
54. Select an element with `id="grid"` and change its `style.gridTemplateColumns` dynamically.
55. Select an element with `id="modal"` and change its `style.zIndex` to bring it to the front.

---

## Level 4 — Functions + DOM (Challenges 56–75)

56. Write a function `updateDisplay(value)` that selects an element with `id="display"` and sets its `textContent` to the passed value.
57. Write a function `toggleVisibility(id)` that toggles the `style.display` of the element between `"none"` and `"block"`.
58. Write a function `setColor(id, color)` that changes the `style.color` of the element with the given id.
59. Write a function `appendText(id, text)` that appends the given text to the existing `textContent` of the element.
60. Write a function `clearInput(id)` that sets the `value` of an input element to an empty string.
61. Write a function `getInputValue(id)` that returns the current `value` of an input element.
62. Write a function `validateInput(id)` that returns `true` if the input's value is not empty, otherwise `false`.
63. Write a function `updateCounter(id, delta)` that reads the current number from an element, adds `delta`, and writes it back.
64. Write a function `formatCurrency(id, amount)` that displays a number formatted as currency in the element.
65. Write a function `showError(id, message)` that sets the text and adds an `"error"` class to the element.
66. Write a function `hideError(id)` that clears the text and removes the `"error"` class.
67. Write a function `disableButton(id)` that sets the `disabled` property of a button to `true`.
68. Write a function `enableButton(id)` that sets the `disabled` property of a button to `false`.
69. Write a function `setLoading(id, isLoading)` that changes the button text to `"Loading..."` when `isLoading` is `true`, otherwise `"Submit"`.
70. Write a function `updateMultiple(ids, value)` that sets the same `textContent` on multiple elements by their ids.
71. Write a function `swapText(id1, id2)` that swaps the `textContent` between two elements.
72. Write a function `incrementUntil(id, max)` that increments the element's number every second until it reaches `max`.
73. Write a function `syncInputs(sourceId, targetId)` that copies the value from one input to another in real time.
74. Write a function `batchUpdate(elements, property, value)` that updates a given property on an array of element objects.
75. Write a function `conditionalRender(id, condition, trueText, falseText)` that shows different text based on a boolean condition.

---

## Level 5 — Arrays + DOM (Challenges 76–95)

76. Given an array of strings, render each string as a list item inside a `ul` with `id="list"`.
77. Given an array of numbers, render each number inside its own `div` with class `"number-card"`.
78. Given an array of objects with `name` and `age`, render a table row for each object inside a `tbody` with `id="table-body"`.
79. Add a new item to an array and re-render the list inside the DOM.
80. Remove an item from an array by index and re-render the list.
81. Remove an item from an array by a property value and re-render the list.
82. Toggle a boolean property on an object in an array and re-render only that item's UI.
83. Sort an array alphabetically and re-render the list in the new order.
84. Filter an array to only show items where a property is `true`, and render the filtered list.
85. Search an array by a string query and render only matching items.
86. Render an array of objects as cards, each with a delete button that removes the item from the array and the DOM.
87. Render an array with a numbered index displayed before each item.
88. Render an array where every other item has a different background color.
89. Render an array in reverse order without mutating the original array.
90. Render an array of objects grouped by a category property into separate sections.
91. Render an array where clicking an item moves it to the top of the list.
92. Render an array with a "Load More" button that shows 5 more items each time it is clicked.
93. Render an array and highlight the item with the highest numeric value.
94. Render an array and allow items to be reordered using up and down buttons.
95. Render an array and persist the array order in a variable after each reorder operation.

---

## Level 6 — Objects + DOM (Challenges 96–110)

96. Given an object with `firstName`, `lastName`, and `email`, render each property in its own DOM element.
97. Given an object with `title`, `description`, and `completed`, render a card that shows all properties.
98. Update a single property on an object and re-render only the DOM element that displays that property.
99. Add a new property to an object and render it in a new DOM element without rebuilding the entire UI.
100. Delete a property from an object and remove its corresponding DOM element.
101. Render an object where boolean properties control the visibility of certain DOM elements.
102. Render an object and allow inline editing: clicking text turns it into an input, saving updates the object.
103. Render an object and display a computed property (e.g., full name from first and last) in the DOM.
104. Render an object and color-code values based on ranges (e.g., low/medium/high scores).
105. Render an object where nested arrays are displayed as comma-separated text.
106. Render an object and show a timestamp of when it was last updated.
107. Render an object and disable editing if a `locked` property is `true`.
108. Render an object and show a progress bar based on a numeric property and its maximum.
109. Render an object and allow the user to clone it, creating a duplicate in the DOM and a duplicate in data.
110. Render an object and display validation errors next to properties that fail custom rules.

---

## Level 7 — Nested Objects + DOM (Challenges 111–125)

111. Given an object with a nested `address` object (street, city, zip), render the full address in a single paragraph.
112. Given an object with a nested `profile` object (avatar, bio, social), render each nested property in its own element.
113. Update a nested property (e.g., `user.address.city`) and re-render only the address section.
114. Render a nested object where each level is indented visually in the DOM.
115. Render a nested object as a collapsible tree where clicking a parent toggles visibility of its children.
116. Given an object with nested `scores` (math, science, english), render each subject with a color-coded bar.
117. Given an object with nested `permissions`, render checkboxes that reflect each boolean permission.
118. Update a deeply nested property and ensure the change propagates to the correct DOM element.
119. Render a nested object where arrays inside objects are rendered as sub-lists.
120. Render a nested object and allow the user to add a new key-value pair at any nesting level.
121. Render a nested object and show the total count of all nested arrays combined.
122. Render a nested object where missing properties show `"N/A"` instead of `undefined`.
123. Render a nested object and allow the user to delete an entire nested object from the parent.
124. Render a nested object and highlight properties that have been modified since initial render.
125. Render a nested object and display the full path to each property as a data attribute.

---

## Level 8 — Classes + DOM (Challenges 126–145)

126. Create a `Counter` class that stores a count and has a `render(elementId)` method to display it.
127. Create a `Counter` class with `increment()` and `decrement()` methods that update the DOM.
128. Create a `Toggle` class that manages a boolean state and updates a button's text between `"On"` and `"Off"`.
129. Create a `Timer` class that counts down from a given number and updates the DOM every second.
130. Create a `TodoItem` class with `text`, `completed`, and methods to `toggle()` and `render()` itself.
131. Create a `TodoList` class that manages an array of `TodoItem` instances and renders the full list.
132. Create a `Modal` class that controls a DOM element's visibility with `open()` and `close()` methods.
133. Create a `FormValidator` class that takes form input ids and validates them against rules.
134. Create a `TabController` class that manages multiple content panels and shows only the active one.
135. Create a `Accordion` class where each section can expand and collapse independently.
136. Create a `Carousel` class that cycles through an array of items and renders one at a time.
137. Create a `Notification` class that creates a temporary DOM element that auto-removes after 3 seconds.
138. Create a `DataTable` class that renders an array of objects as a sortable table.
139. Create a `Chart` class that renders a simple bar chart from an array of numbers using DOM elements.
140. Create a `DragList` class that allows reordering list items via click-to-move buttons.
141. Create a `SearchFilter` class that filters a list of DOM elements based on an input query.
142. Create a `Pagination` class that splits an array into pages and renders page controls.
143. Create a `ThemeSwitcher` class that toggles between light and dark themes by changing CSS classes on the body.
144. Create a `Character` class with stats (HP, MP, STR) and methods to take damage, heal, and level up.
145. Create a `CharacterManager` class that manages multiple `Character` instances and renders them as cards.

---

## Level 9 — Forms (Challenges 146–165)

146. Select a text input with `id="username"` and display its value in a paragraph when a button is clicked.
147. Select a password input and toggle its type between `"password"` and `"text"` when a checkbox is clicked.
148. Select a select dropdown and display the selected option's text (not value) in a div.
149. Select a textarea and display a live character count below it.
150. Select a group of checkboxes and display how many are checked.
151. Select a group of radio buttons and display the selected value only when one is selected.
152. Select a color input and change the page background to the selected color in real time.
153. Select a range input and display its numeric value next to it as it slides.
154. Select a date input and calculate and display the number of days between the selected date and today.
155. Select a file input and display the name of the selected file.
156. Validate that a text input is not empty before allowing form submission.
157. Validate that an email input contains both `"@"` and `"."` before submission.
158. Validate that a password input is at least 8 characters long.
159. Validate that two password inputs match each other.
160. Validate that a number input is within a min-max range.
161. Prevent form submission using `preventDefault()` and display all form values in a summary div instead.
162. Clear all form inputs when a `"Reset"` button is clicked.
163. Serialize all form inputs into a single object and log it.
164. Show field-specific error messages below each invalid input.
165. Disable the submit button until all required fields are valid.

---

## Level 10 — Dynamic DOM Creation (Challenges 166–180)

166. Use `document.createElement` to create a `div`, set its `textContent`, and append it to `id="container"`.
167. Create a `button` element dynamically, set its text, and attach a `click` event listener before appending.
168. Create a `ul` and populate it with `li` elements created in a loop from an array.
169. Create a table with `thead` and `tbody` entirely from JavaScript using nested `createElement` calls.
170. Remove the last child element from a container using `removeChild`.
171. Remove a specific element by its id using `remove()`.
172. Replace an existing element with a new one using `replaceChild`.
173. Insert a new element before an existing element using `insertBefore`.
174. Clear all children from a container using a `while` loop with `removeChild`.
175. Clone an existing element using `cloneNode` and append the clone with modified text.
176. Create a modal overlay dynamically with a backdrop and a content box, then remove it on click.
177. Create a tooltip element dynamically that follows the mouse cursor.
178. Create a notification toast element that slides in and removes itself after 3 seconds.
179. Create a loading spinner element dynamically and insert it while data is "loading".
180. Create a dropdown menu dynamically from an array of options and append it to a header.

---

## Level 11 — Event Delegation (Challenges 181–195)

181. Attach a single `click` listener to a `ul` with `id="list"` and use `event.target` to determine which `li` was clicked.
182. Attach a single `click` listener to a `table` and determine which row's delete button was clicked using `closest()`.
183. Attach a single `click` listener to a container of dynamically generated buttons and identify which button was clicked.
184. Use event delegation to toggle a class on any clicked element inside a container, regardless of when it was added.
185. Use event delegation to handle clicks on pagination buttons that are re-rendered on every page change.
186. Use event delegation on a form container to handle input validation for dynamically added fields.
187. Use event delegation to implement a "remove" action for list items that are added after the initial render.
188. Use event delegation to implement sorting by clicking table headers, even when rows are re-rendered.
189. Use event delegation to handle keyboard shortcuts on a dynamic list of editable items.
190. Use event delegation to implement a "select all" checkbox that controls dynamically rendered child checkboxes.
191. Use event delegation to show a context menu on right-click for any item in a dynamic grid.
192. Use event delegation to implement drag-and-drop reordering indicators on a dynamic list.
193. Use event delegation to handle focus events on inputs that are added and removed dynamically.
194. Use event delegation to implement a master-detail view where clicking any item in a list updates a detail panel.
195. Use event delegation to implement batch actions (delete selected, mark completed) on a dynamic list.

---

## Level 12 — State Management Thinking (Challenges 196–210)

196. Maintain an array of todo objects in a variable. Never read the DOM to determine the state. Always update the variable first, then re-render.
197. Implement a function that updates a state object and then calls a `render()` function to sync the DOM.
198. Create a state object with `users`, `currentUser`, and `isLoggedIn`. Write a `renderApp()` function that shows/hides UI based on state.
199. Implement undo functionality: keep a history array of previous states and restore the DOM to a previous state.
200. Implement a state-driven tab system: the active tab is stored in a variable, and `render()` updates which panel is visible.
201. Implement a shopping cart state: an array of items with quantities. The DOM should always reflect the cart state, never the other way around.
202. Implement a filter state: store the current filter in a variable, and re-render the list whenever the filter changes.
203. Implement a search state: store the query in a variable, and re-render results whenever the query changes.
204. Implement a pagination state: store `currentPage` and `itemsPerPage`, and render only the slice of data for that page.
205. Implement a sort state: store `sortKey` and `sortDirection`, and re-render the list in the correct order.
206. Implement a form state: store form values in an object as the user types, and only read from that object on submit.
207. Implement a notification state: an array of messages. Add, remove, and render notifications without querying the DOM.
208. Implement a loading state: a boolean `isLoading`. Show/hide a spinner and disable buttons based on this state alone.
209. Implement a theme state: `"light"` or `"dark"`. Apply a class to the body based on state, and save the state to `localStorage`.
210. Implement a modal state: a `modal` object with `isOpen`, `title`, and `content`. Render the modal from this object only.

---

## Level 13 — Mini Projects (Challenges 211–240)

211. **Counter App**: Build a counter with increment, decrement, and reset buttons. Display the count. Prevent negative values.
212. **Counter App**: Add a feature to set the counter to a custom value via an input field.
213. **Counter App**: Add a feature to auto-increment every second with start/stop controls.
214. **Calculator**: Build a calculator that can add, subtract, multiply, and divide two numbers from inputs.
215. **Calculator**: Add a clear button and prevent division by zero with an error message.
216. **Calculator**: Add keyboard support so number keys and operators work without clicking.
217. **Todo App**: Build a todo app where you can add items from an input, mark them complete, and delete them.
218. **Todo App**: Add a filter to show all, active, or completed todos.
219. **Todo App**: Add a "Clear Completed" button that removes all completed items.
220. **Todo App**: Add a counter that shows the number of active todos remaining.
221. **Notes App**: Build a notes app where you can create, edit, and delete notes. Each note has a title and body.
222. **Notes App**: Add a search feature that filters notes by title or body content.
223. **Notes App**: Add a feature to pin notes so they appear at the top.
224. **Student Manager**: Build an app to add students (name, grade) to a table, remove them, and show the average grade.
225. **Student Manager**: Add validation so names must be at least 2 characters and grades must be 0–100.
226. **Student Manager**: Add a feature to highlight the top student.
227. **Expense Tracker**: Build an app to add expenses (name, amount, category) and display them in a list.
228. **Expense Tracker**: Show the total amount spent and the total per category.
229. **Expense Tracker**: Add a feature to filter expenses by category.
230. **Inventory Manager**: Build an app to add items (name, quantity, price) and display them as cards.
231. **Inventory Manager**: Add restock and sell buttons that update quantity.
232. **Inventory Manager**: Highlight items with low stock (quantity < 5) in red.
233. **Quiz App**: Build a quiz with multiple choice questions. Track score and show results at the end.
234. **Quiz App**: Add a progress indicator showing current question out of total.
235. **Quiz App**: Add a review screen showing which questions were answered correctly or incorrectly.
236. **Habit Tracker**: Build an app to track daily habits. Each habit can be marked done or not done.
237. **Habit Tracker**: Show a streak count for each habit based on consecutive days completed.
238. **Habit Tracker**: Add a weekly view showing which days each habit was completed.
239. **Character Manager**: Build an RPG-style character manager with classes (Warrior, Mage, Rogue), stats, and level-up.
240. **Character Manager**: Add actions (heal, damage, gain XP) that update stats and trigger visual changes.

---

## Level 14 — Advanced DOM Thinking (Challenges 241–260)

241. Separate all data logic from all DOM logic in an application. Data functions should never touch the DOM. DOM functions should never modify data directly.
242. Build an application where the entire UI is rendered from a single state object. There is no DOM manipulation outside the render function.
243. Implement a pub-sub pattern: an `eventBus` object where components can subscribe to events and publish updates, decoupling DOM updates from data changes.
244. Build a reusable `Component` base class with `render()`, `mount()`, and `destroy()` methods. Extend it for specific UI pieces.
245. Implement a virtual DOM diffing system: compare a new state tree to the current DOM and only update changed elements.
246. Build an autocomplete component that fetches suggestions from an array, renders them dynamically, and handles keyboard navigation.
247. Build an infinite scroll list: detect when the user scrolls to the bottom and append more items from a large array.
248. Implement a drag-and-drop list reordering system using mouse events, updating both the DOM and the underlying array.
249. Build a resizable panel layout using mouse events to adjust widths, storing the layout in state.
250. Implement a debounced search: wait for the user to stop typing before re-rendering search results.
251. Build a form wizard: a multi-step form where each step is a separate component, and state persists across steps.
252. Implement undo/redo for a complex application using command objects that encapsulate each action and its inverse.
253. Build a collaborative-style UI where multiple "users" (simulated) can edit the same list, showing conflicts.
254. Implement accessibility: ensure all interactive elements have proper ARIA attributes, focus management, and keyboard navigation.
255. Build a responsive dashboard that re-renders its layout based on window size, stored in state.
256. Implement optimistic UI updates: update the DOM immediately on user action, then reconcile if the server response differs.
257. Build a memory-efficient list that only renders visible items (virtual scrolling) for a dataset of 10,000 items.
258. Implement a custom event system: create and dispatch custom events to communicate between unrelated components.
259. Build a plugin architecture: an app core that allows registering plugins, each plugin adding its own DOM and behavior.
260. Build a complete single-page application with routing: change the visible "page" based on a route state, without reloading.

---

**Total Challenges: 260**

Work through these in order. Each challenge builds on the previous. Do not look up solutions until you have attempted the problem yourself. The goal is to develop problem-solving intuition, not to memorize syntax.
